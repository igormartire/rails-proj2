<h1>New Item</h1>

<%= simple_form_for @item do |f| %>
  <%= f.input :name, label: 'Item Name:', error: 'Name is mandatory' %>
  <%= f.input :price, as: :integer%>
  <%= f.input :description, as: :text %>
  <%= f.input :avatar, as: :file %>
  <%= f.button :submit %>
<% end %>
